# 数据域知识文档:<数据域/项目名>

> 本文档沉淀**已确认**的可复用知识,跨会话累积。每次会话经用户同意后增量更新,不覆盖既有内容。

## 表清单

### 表:<表名>

- **用途**:<这张表是干什么的>
- **字段**:
  - `<字段名>` `<类型>` —— <业务口径/含义>
  - `<字段名>` `<类型>` —— <业务口径/含义>
- **备注**:<分区键、更新频率、数据量级等,可选>

### 表:<表名>

- **用途**:...
- **字段**:...

## 关联关系

> 形如 `A.col ↔ B.col  [基数]  说明`。基数取 `1:1` 或 `1:N`(N 在多的一侧)。

- `dim_student.student_id` ↔ `fact_exam_score.student_id`  `1:N`  一个学生多条成绩
- `dim_class.class_id` ↔ `dim_student.class_id`  `1:N`  一个班级多个学生

## 字段口径 / 枚举(可选)

- `<字段>` 取值:`0=未缺考`,`1=缺考`
- `<指标>` 计算口径:...
